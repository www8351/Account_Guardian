# Fix proof, build 72ed960, 2026-07-30 night

Deployed 22:39:30, compiled `Result: 0 errors, 0 warnings`, ex5 39038 bytes. Loaded into the terminal by the chart 1 attach at 22:50:17.

Build identification, primary marker as ruled by the owner: the only ex5 on disk was written at 22:39:30, and MT5 loads the ex5 from disk at attach time, so any attach after that timestamp cannot be running 694d570. Corroboration: LIFE lines from 22:50:18 onward carry the two fields 694d570 never emitted, `|server=...|local=...`.

## Baseline

Halt file md5 `0D7C84933F9214D7BAE9E38E195A29EB`, 91 bytes, mtime 22:50:17, three session records. Taken immediately before the first tested action, not at an earlier convenient moment. The file is stable while an instance runs because AgHaltSave is called only at init, at deinit, and on SAFE_HALT entry, never from the timer.

## Refusals observed, all on 72ed960, all fresh images

| time | chart | path | alert |
|---|---|---|---|
| 22:53:33.309 | XAUUSD.ecn M5 | mutex | another AccountGuardian instance is live |
| 22:53:37.079 | XAUUSD.ecn M5 | mutex | another AccountGuardian instance is live |
| 22:54:00.758 | US100.ecn M5 | config | both limits are zero |
| 22:54:17.865 | XAUUSD.ecn M5 | mutex | another AccountGuardian instance is live |
| 22:55:28.976 | US500.ecn M5 | mutex | another AccountGuardian instance is live |
| 22:55:39.976 | US500.ecn M5 | config | both limits are zero |

Four distinct mutex refusals and two config refusals. Counting note: each refusal emits two matching journal lines, the structured `ALERT` line and the terminal's own `Alert:` echo, so a naive grep count doubles them.

Every one of the six logged, verbatim and identically:

```
INFO|deinit|halt file NOT written: the halt model was never loaded this session (init refused before AgHaltLoad), so writing it would erase the session history and any persisted halt flag
INFO|deinit|reason=8|session marked clean|timer_armed=0|timer_ticks=0
```

`timer_armed=0|timer_ticks=0` on all six confirms each ran in a fresh program image, which is the condition under which 694d570 erased the file.

## Result

Halt file after all six refusals: md5 `0D7C84933F9214D7BAE9E38E195A29EB`, 91 bytes, **mtime still 22:50:17**. Byte-identical AND never opened for writing. An unchanged mtime is the stronger claim: nothing wrote at all, rather than something writing identical bytes.

Still three session records, so no refused init appended one. That independently confirms refusals cannot accumulate toward the crash-loop count, since AgHaltAppendSession sits downstream of every refusal return.

FIX PROVEN on both refusal paths, config and mutex, in fresh images. Contrast with 694d570 earlier the same evening, where a single fresh-image config refusal at 22:26:37 erased six session records.

Scope limit, unchanged: this is the erasure half. The false RESUMED_FROM_SAFE_HALT half needs a persisted SAFE_HALT that only A3 can produce, and stays source-read-only.

## A1, NOT closed

What tonight's four mutex refusals DO close, on 72ed960:
- a second instance refuses rather than taking over, while a live holder exists
- the refusal raises an Alert popup and a structured ALERT journal line naming the account
- the first instance is unaffected: chart 1's LIFE cadence ran through every refusal at exact 30 s spacing
- the halt file is untouched by the refusal

What remains open:
- the chart banner state. `AgBanner("REFUSED", "duplicate instance", 0, ...)` writes to the chart, not the journal, and the extra charts were closed before anyone looked, so it is unobservable in retrospect.
- the takeover-versus-refuse distinction. These refusals prove the live-holder branch only; the stale-heartbeat takeover branch is A2 and needs a hard kill.

A1 therefore runs deliberately tomorrow with valid inputs on a second chart and the banner checked before detaching. Tonight's rows stand as substantiating evidence with the timestamps above.

## Step 6 did not execute as intended

The owner pressed F7 on chart 1, loaded a9_defaults.set and clicked OK, intending to reset last-used inputs to the defaults. No journal line resulted: no deinit reason=5, no re-init, and the halt file mtime stayed at 22:50:17. Chart 1's `seconds_in_state` continued unbroken to 451 at 22:57:48, which counts back exactly to the 22:50:17 init.

Interpretation: chart 1 already held those exact values from the 22:50:17 attach, so MT5 saw no input change and did not re-initialize the EA.

Consequence, stated as a gap rather than papered over: whether the last-used input store now holds 5.0 and 0.0 is UNVERIFIED. The last attach MT5 processed was the 22:55:39 US500 config refusal carrying both zeros. Last-used seeds new Navigator attaches; a terminal relaunch instead restores each chart from its profile-stored inputs, which for chart 1 are the defaults it was attached with. So the overnight relaunch risk is probably low, but "probably" is inference, not measurement, and it is recorded as such.

Bounding the exposure: the risk only materialises if the terminal relaunches before the A8 window closes at about 00:53. Last night's machine death was at about 03:28, after that point.

## A8 window status

LIFE cadence became continuous and uninterrupted at **22:50:18**, the first LIFE line of the instance that started at 22:50:17, and ran unbroken through 22:57:48 at exact 30 s spacing with both clock fields present. Step 6 did not interrupt it, since it never re-inited.

So the row's continuity precondition is satisfied from 22:50:18, ahead of the 23:01 window opening rather than at it. The 15-minute no-tick clock starts when the `server=` field stops advancing while `local=` keeps advancing, which is the measurement to harvest from the log tomorrow. The independent second measure, `timer_ticks` on a deinit line, arrives with A2's first hard kill.
